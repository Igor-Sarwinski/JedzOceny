import React from 'react';
import { Box, Text } from '@gluestack-ui/themed';

export const AddPlace = ({navigation}:any) => {
    return (
        <Box flex={1} justifyContent="center" alignItems="center">
            <Text>Add place</Text>
        </Box>
    );
};

export default AddPlace;
