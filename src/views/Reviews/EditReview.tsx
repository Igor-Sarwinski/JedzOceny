import React from 'react';
import { Box, Text } from '@gluestack-ui/themed';

export const EditReview = ({navigation}:any) => {
    return (
        <Box flex={1} justifyContent="center" alignItems="center">
            <Text>Edit Review</Text>
        </Box>
    );
};

export default EditReview;
