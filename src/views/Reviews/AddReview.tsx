import React from 'react';
import { Box, Text } from '@gluestack-ui/themed';

export const AddReview = ({navigation}:any) => {
    return (
        <Box flex={1} justifyContent="center" alignItems="center">
            <Text>Add review</Text>
        </Box>
    );
};

export default AddReview;
